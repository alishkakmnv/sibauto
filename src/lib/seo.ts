/**
 * Демо не должно попадать в поиск: на страницах стоит реальный телефон клиента,
 * а согласия на публикацию ещё нет.
 *
 * Индексация включается ровно одним способом — переменной окружения
 * `NEXT_PUBLIC_ALLOW_INDEXING=true` на боевом домене. По умолчанию (и на всех
 * превью Vercel) сайт закрыт.
 *
 * Важно: robots.txt при этом НЕ запрещает обход. Если закрыть обход, краулер
 * не увидит noindex и сможет проиндексировать сам URL по внешней ссылке.
 * Правильный способ — пустить робота и отдать ему запрет: заголовком
 * X-Robots-Tag (см. next.config.ts) и мета-тегом (см. app/layout.tsx).
 */
export const allowIndexing = process.env.NEXT_PUBLIC_ALLOW_INDEXING === "true";
